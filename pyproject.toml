[project]
name = "3d-studio-demo"
version = "0.1.0"
description = "Reconstruct editable 3D spaces from photos through structured scene data and Blender."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
